apiVersion: v1
kind: Service
metadata:
  name: {{ include "serialmonitor.fullname" . }}
  labels:
    {{- include "serialmonitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "serialmonitor.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
